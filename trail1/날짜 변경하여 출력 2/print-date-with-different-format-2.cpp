#include <iostream>
using namespace std;

int main() {
    // 변수 선언 및 입력
    int y, d, m;
    cin >> m;
    cin.get();
    cin >> d;
    cin.get();
    cin >> y;

    // 출력
    cout << y << '.' << m << '.' << d;
    return 0;
}